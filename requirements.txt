librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.21.0
scipy>=1.9.0
matplotlib>=3.5.0
sounddevice>=0.4.0
dearpygui>=1.10.0

# Optional GUI backends for visual downbeat selection and interactive beatgrid alignment
# Install one of these for --manual-downbeats and --interactive-beats to work:
# PyQt5>=5.12.0     # Alternative to tkinter
# tkinter           # Usually included with Python

# Dear PyGui is now the primary GUI backend for interactive beatgrid alignment
# providing GPU-accelerated, real-time performance for professional audio applications